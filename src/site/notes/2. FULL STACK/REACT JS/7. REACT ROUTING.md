---
{"dg-publish":true,"permalink":"/2-full-stack/react-js/7-react-routing/","noteIcon":""}
---

#react #reactjs #JS #javascript 

- To perform **PAGE ROUTING** you need to install **`REACT ROUTER`** module.
- Use the below the *command* to install the module. 
	- **`npm install react-router-dom`**
***
*** 
# PAGE ROUTING SETUP 
## STEPs 
1. Go to **`main.jsx`** 
	 - Here,  we need to wrap the **`<App/>`** *component* to link with the *page route*
	 - Therefore, we use a *component* name **`BrowserRouter`** 
	 - This will allow us to use *directory traversal*  (i.e. `weblink/page_name`) functionality.
```JSX 
import { BrowserRouter } from "react-router-dom";
createRoot(document.getElementById("root")).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>
);
```

2. Go to the **`App.jsx`** 
	- Here, we'll create a **ROUTER** 
	- Which allows us to *define* which *component* to be displayed.
	 - Now, Add the **`<main> </main>`** div here. 
	 - Inside *main div*, Add the **`Router`** & **`Route`** div to use the *page routing* 
```JSX 
<Routes>
	<Route path="/" element= {Component_Name} />
</Routes>
```

```JSX 
import { Route, Routes } from "react-router-dom";

function App() {
  return (
    <main className="main-content">
      <Routes>
        <Route path="/" element={<Home />} />
         <Route path="/aboutus" element={<AboutUs />} />
      </Routes>
    </main>
  );
}
```

***
***
# LINKS 
- It's same like the **`href`** in HTML. 
- **`<Link to=path> page_title </Link>`**

```JSX
<Link to="/" className="nav-link">Home</Link>
```

```JSX
import { Link } from "react-router-dom";

export function NavBar() {
    <nav className="navbar">
        <div className="navbar-brand">
            <Link to="/">
            Movie App
            </Link>
        </div>
     </nav>
```