---
{"dg-publish":true,"permalink":"/2-full-stack/java-oops/9-java-abstraction/","noteIcon":""}
---

- It's used to **HIDE** the **IMPLEMENTION DATA**. 
- In other words, *to HIDE unnecessary details & Only show the essential parts/functionalities to the user.*
- The implementation data will be hidden in child class.
- **ABSTRACT CLASS** is faster in performance cause it has to traverse less.
***
# TYPES OF ABSTRACTION: -
1. **ABSTRACT CLASS**
2. **INTERFACEs** 
## 1. ABSTRACT CLASS (PARTIAL-ABSTRACTION)
1. It's can declared by using **ABSTRACT** keyword.
	  - **`public abstract class cls_name{}`**	 
 2. It consists of 
	- **ABSTRACT FUNCTION** - *It's only declared but doesn't consists any body (defined)*.
		- **`public abstract void func_name();`**
	- **NON - ABSTRACT FUNCTION** - *It's declared & also contains body. Just like an normal function & doesn't have any* **ABSTRACT KEYWORD**
		- **`public void func_name(){block of code};`**

3. Can Have *MULTIPLE ABSTRACT & NON-ABSTRACT METHOD*
4. You can't Create the **OBJECT** of an **ABSTRACT CLASS**. 
	 - But, you can give the **REFERENCE** of the *ABSTRACT CLASS*
5. It can have any **Type of Variables** *(Public, Final, Static)*
***
#### Example - 
```java
public abstract class Human {

// abstract function/method
	public abstract void walk(); //Mandatory 

// non - abstract function/method
	public void breath() {     
		System.out.println("I can breath"); 
	} 
}
```
***
> [!NOTE]+
>  - *Whenever you **EXTENDS** an **ABSTRACT CLASS** *,
> 	 - it's*  **MANDATORY** *to* **OVER-RIDE** *all the* **FUNCTION** *of the* **PARENT CLASS**  (i.e. ABSTRACT CLASS).
> 	- Meaning -  Creating a same non-abstract method in the child class with body) 

```Java
// HUMAN CLASS - Parent Class
public abstract class Human {
	// ABSTRACT method
	public abstract void walk();
	
	public void breath() {     
		System.out.println("I can breath"); 
	} 
}
```

``` Java
// BOY CLASS - Child Class
----------------------------------------------------------
public class Boy extends Human{

// Here, you need to again declare & define(body) the abstract function/method

	@Overriden
	public void walk(){
	System.out.println("I can Walk"); 

// We can also have Non-overriden method
	public void talk(){
	System.out.println("I can Talk");
		}	
	}
}
```

```Java 
//Main class
----------------------------------------------------------
// Abstract Class Object Creation 
Human a1 = new Human();  // Give an error

//Simple object creation
Boy b1 = new Boy();
b1.walk();
b1.talk();

//Dynamic Polymorphism
Human h1 = new Boy();
h1.walk();
h1.talk();// Can't call the this Object.
```
***
***