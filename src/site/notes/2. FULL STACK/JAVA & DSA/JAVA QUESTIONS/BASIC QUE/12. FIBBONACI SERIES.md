---
{"dg-publish":true,"permalink":"/2-full-stack/java-and-dsa/java-questions/basic-que/12-fibbonaci-series/","noteIcon":""}
---

- The logic is that the 1st number becomes second & second becomes third & so on.
```Java
public static void fib(){  
    Scanner input = new Scanner(System.in);  
    System.out.print("Enter the number: ");  
    int num = input.nextInt();  
  
    int first_term=0;  
    int second_term=1;  
    int sum= 0;  
  
    for (int i = 0; i < num ; i++) {  
        System.out.print(first_term+",");  
        sum = first_term+second_term;  
        first_term = second_term;  
        second_term = sum;  
    }  
}
```