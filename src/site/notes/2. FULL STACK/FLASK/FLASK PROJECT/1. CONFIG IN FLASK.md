---
{"dg-publish":true,"permalink":"/2-full-stack/flask/flask-project/1-config-in-flask/","noteIcon":""}
---

#python #flask 

- The first step is to setup the **CONFIGURATION** of the *FLASK* App. 

```Python 
from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask_cors import CORS

# Intialization of flask app
app = Flask(__name__)

# using CORS we can send Cross origin request to our app
CORS(app)

  

# Database configuration
app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///mydb.db"
app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False

# Instance creation of Database
db = SQLAlchemy(app)
```
