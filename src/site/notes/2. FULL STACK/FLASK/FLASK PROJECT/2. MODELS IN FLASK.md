---
{"dg-publish":true,"permalink":"/2-full-stack/flask/flask-project/2-models-in-flask/","noteIcon":""}
---

#flask #python 

- Step 2 is to create a **MODEL** for the web - app.
	1. Import **`db`** from **`config`** file.
	2. Make the **MODEL CLASS** 
	3. Add the **ATTRIBUTEs** 
	4. Define a *function* to keep data in **JSON** 
```Python 
from config import db

class Contact(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    first_name = db.Column(db.String(80), unique=False, nullable= False)
    last_name = db.Column(db.String(80), unique=False, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    
    # making the data into the JSON format, For API.
    def to_json(self):
        return{
            "id" : self.id,
            "firstName" : self.first_name,
            "lastName" : self.last_name,
            "email" : self.email
        }
```